document.getElementById('btnSave').addEventListener('click', saveCard);
let Cards = new Array();

function saveCard(){
    let frontCard = document.getElementById('frontCard').nodeValue;
    let backCard = document.getElementById('backCard').nodeValue;
    let card = {front: frontContent, back: backContent}
}